services:
  - type: web
    name: expenseiq-api
    env: python
    rootDir: server
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server.wsgi:app
    healthCheckPath: /health
